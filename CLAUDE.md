@AGENTS.md

<!--
This file is a pointer and nothing else. The guidance lives in AGENTS.md, which is
tool-neutral and readable by any agent; the one line above imports it.

It exists because the harness auto-loads a file of THIS NAME and no other, and a
session that forgets to name MAINTAINING.md would otherwise start with no
instructions at all. The owner weighed that against the zero-trace invariant on
2026-07-30 and chose to keep it. The carve-out is recorded in the invariant itself
in MAINTAINING.md — read it there before concluding this file is a violation and
deleting it. It is not; it is the single, deliberate exception.

Keep it to the import. Content belongs in AGENTS.md, so that nothing but the
filename ever carries the exception.
-->

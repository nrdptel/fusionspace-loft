# Loft — Competitive Ledger

**The purpose of this file is to make "compete with OpenRocket" a tracked gap instead of a mood.**

`MAINTAINING.md` sets the bar as "Loft has to feel like it belongs in that company" and the done-check
asks each run to benchmark one surface against a mature tool. Until this file existed, that benchmark
went into a chat report nobody read and nothing accumulated. Now it lands here, one row at a time, and
the rows are where `ROADMAP.md` milestones come from.

## How to use it

1. **Every run adds or resolves at least one row.** The done-check requires it. A run that benchmarks
   a surface and files nothing here did not benchmark it.
2. **A row is a claim until verified.** Rows marked `UNVERIFIED` are leads, not facts. Verify against
   the tool's own documentation, source, or a real run before you build against one — and change the
   marker when you do, with what you checked. Building a feature to match a competitor capability that
   was misremembered is the expensive version of the failure `MAINTAINING.md` already warns about.
3. **`Verdict` is a decision, and decisions are cheap to reverse but expensive to re-derive.** Use:
   - `GAP` — they have it, we want it, it is not yet on the roadmap.
   - `QUEUED` — on `ROADMAP.md`, with the milestone named.
   - `HAVE` — we do this, at least as well. Say what proves it.
   - `BETTER` — we do it better than they do. Say what proves it, because this is the claim that
     matters and the one most easily kidded about.
   - `REJECT` — deliberately not doing it. **Say why**, in one line. A rejection without a reason gets
     re-litigated every run.
4. **A `GAP` that survives two runs should become a milestone or a `REJECT`.** Standing gaps with no
   decision are how a competitive analysis turns into decoration.
5. **Never copy their code.** The CLEAN-ROOM invariant is absolute. This file tracks *capabilities and
   outcomes a flyer can see* — never implementation. Read published formats and papers; never
   GPL-licensed source.

## The field

| Tool | What it is | Why it matters here |
|---|---|---|
| **OpenRocket** | Free desktop design + simulation, Java, GPL | The default. The editor Loft's North Star #2 benchmarks against, and the format most flyers already have. |
| **RocketPy** | Python 6-DOF simulation library | The accuracy benchmark. Real atmospheric data, Monte Carlo dispersion, the physics ceiling to beat. |
| **RASAero II** | Windows aero prediction + flight sim | The supersonic and drag-accuracy benchmark; fin flutter. |
| **RockSim** | Paid desktop design + simulation | The commercial polish benchmark; `.rkt` is a format Loft ingests. |
| **SpaceCAD** | Paid desktop design | Another format, another editor idiom. |
| **ThrustCurve** | Motor database and API | The motor data source flyers expect to be complete and current. |

---

## Ledger

**Append at the bottom; the numbers are stable references and never renumber.** The header used to say
"newest first" while the table ascended 1..19 from the top, which two runs would have resolved two
different ways. A row's number is how `ROADMAP.md` and this file's own notes cite it, so it outlives its
position. `file:line` or a route where ours lives, so the comparison is reproducible.

| # | Tool | Capability | Where ours is | Verdict | Note |
|---|---|---|---|---|---|
| 1 | OpenRocket | **Tabbed workspace** — design, motors & configuration, flight simulations are separate tabs, each dense and complete | Four static routes behind one spine as of 2026-08-02 — `/flight`, `/design`, `/sweep`, `/validate` — over an import root, which is the five distinct jobs P2's *done when* names. `components/LoftApp.tsx` is still **2,867 lines** (measured, not remembered; the 2,577 here was two runs stale): the split moved the routing, not yet the file | `QUEUED` | P2 — Workspaces as routes. This is the single largest structural reason Loft reads as a demo rather than a tool. The PRODUCT SHAPE invariant already forbids the current shape. **Depends on P1** (row 21): converting surfaces onto shared primitives first means the split moves components rather than rewriting them, so P1's progress is partial payment on this row. |
| 2 | OpenRocket | **Component presets** — a database of real commercial parts (tubes, nose cones, couplers) by vendor and part number, picked rather than typed | **RESOLVED 2026-08-03 — all five kinds ship.** Body tube, nose cone, parachute, coupler and centring ring are all pickable from `components/PartPicker.tsx` on `/design`, one component driven by a `kind`, over 2,990 parts from 13 vendors. Earlier state, kept because the trade below was measured against it: **two of five shipped 2026-08-02.** `components/PartPicker.tsx` on `/design`, one component driven by a `kind`: **1,089 body tubes** and **854 nose cones**, searchable by number or description, filterable by vendor or to the design's own caliber. A tube pick sets the vendor's outer diameter, length, wall and stock, and rescales the airframe to the caliber. A cone pick takes the WHOLE published part — contour, base, length, shoulder, wall, stock, and the vendor's own weight where they state one — because the data supports it: 854 of 854 cones state all six, against 0 of 1,089 tubes stating a wall. Measured end to end on `demo-single-deploy.ork` with SEMROC BNC-55D2: dry mass 600.2 → 525.6 g, CG 572.5 → 457.1 mm, static margin 4.065 → 2.7095 cal, apogee 992.8 → 1043.8 m. (CG and margin re-measured 2026-08-13: R12 increment 15 corrected what a stated part weight means for a part's balance point, and a catalogue cone pick writes the vendor's weight as exactly that, so this row's own headline measurement moved. Previously published as 456.9 mm / 2.712 cal.) Where OpenRocket is still ahead: it covers every component type Loft does not yet author (transitions, launch lugs, streamers, engine blocks), and **its presets are filtered by what physically fits the parent** | `RESOLVED` for the five kinds; the FIT clause stays open | High leverage: a flyer building a real rocket is holding a catalogue part. Turns authoring from measurement into selection. Needs a licence-clean data source — verify before scoping. **One sentence in this row was measurably wrong and is corrected here rather than left standing.** It said Loft's caliber filter and OpenRocket's answer the same question at different tolerances; verified against `ComponentPresetRowFilter.java` on 2026-08-03, OpenRocket's is `epsilon = MathUtil.max(value * 0.05, 0.001)` — **5%, floored at 1 mm** — against Loft's flat **0.5 mm**. At a 38 mm airframe theirs admits ±1.9 mm and ours ±0.5 mm, so the "OpenRocket's filter would show a metric HPR builder an empty list too" claim below is understated in Loft's favour and should be read as 18 rows rather than 0 at their tolerance. The conclusion survives, the arithmetic behind it did not. **And the two internal kinds shipped 2026-08-03 do filter on fit** — the host tube's BORE, not the airframe caliber, because that is the dimension a coupler has to match — so the position below is now specific to the two kinds that make up the outer mould line rather than a blanket one.

**On the fit clause for the airframe kinds Loft has taken the opposite position deliberately, and it is worth watching rather than closing.** OpenRocket filters the offer to what fits; Loft offers the part, takes it faithfully, and lets the mould-line-step check it already had (row 22) name the consequence — a 39.95 mm cone on a 38.0 mm tube flies with *"changes diameter at a joint… the drag is under-counted"* rather than being silently resized to fit. The measurement that makes this a real trade rather than a rationalisation: **0 of 854 cones** sit within the picker's own 0.5 mm of a 38.0 mm airframe (0 at 1 mm, 18 at 2 mm, 231 at 5 mm), across 103 distinct base diameters from 7.14 to 296.16 mm — the catalogue is imperial stock, so OpenRocket's filter would show a metric HPR builder an empty list too. Filtering is the better affordance where stock exists; naming the step is the better answer where it does not. |
| 3 | OpenRocket | **Materials database** with density by material, so component mass is derived rather than entered | Mass entered or defaulted | `GAP` | Prerequisite for presets (2) being useful. Small, and it makes every mass number better grounded. |
| 4 | OpenRocket | **Printable templates** — fin planforms, centring rings and transition wraps exported to scale for cutting | Nothing | `GAP` | `UNVERIFIED` at the detail level; the capability exists. Genuinely differentiating and entirely client-side. A flyer prints these and builds against them. |
| 5 | OpenRocket | **Design warnings panel** — a standing list of what is wrong or questionable about the design, updated live | Validation exists but is not a persistent live panel over the design | `GAP` | Fits the SAFETY posture exactly: honest flags, no verdict. |
| 6 | OpenRocket | **Undo/redo across the whole design** | Shipped — `lib/model/history.test.ts`, 17 cases | `HAVE` | R2. Verified by e2e including gesture coalescing. |
| 7 | OpenRocket | **Component tree: add, remove, reorder, restack** | **RESOLVED 2026-08-08 for the four MECHANICS.** Add shipped R3, remove R2, reorder and restack R4 (2026-07-31) — pinned by `moveSlots`, 484 drop slots and 206 reorders driven across all 35 real designs, and by four e2e cases. This row read `QUEUED` for eight days after R4 shipped, while row 20 recorded the same capability as `HAVE`; two rows disagreeing about one milestone is exactly what this ledger exists to prevent, so the lesson is the same as the roadmap's own track header — resolve the row in the commit that ships the milestone | `HAVE` for the mechanics; **the SURFACE is now row 39** | R4. **The mechanics are not the tree.** OpenRocket's flyer sees a nested outline and acts on it; Loft's tree exists in the model (`children` on every component) and has no UI, so these four operations are reached through a diagram and a flat collapsed table. The owner named that gap directly on 2026-08-08 (`OWNER-NOTES.md` `ON-6`), and it is split out rather than folded in here, because "can Loft reorder a part" and "can a flyer see the structure they are reordering" have different answers. |
| 8 | RocketPy | **Monte Carlo dispersion with real atmospheric data** (reanalysis / forecast wind profiles), not a synthetic distribution | **Loft fetches real winds aloft and disperses over them.** `lib/weather.ts` calls the Open-Meteo Forecast API (GFS/HRRR seamless) for PRESSURE-LEVEL wind fields and builds a profile, taking each level's altitude from the geopotential height; `components/MonteCarlo.tsx` carries a `windProfile` through its own cache key, and under today's weather the solver reads that profile and never looks at a surface wind at all | `HAVE` | **RESOLVED 2026-08-03 — the row's claim that "the wind model is not real-data driven" was REFUTED by reading the code it describes.** Verified in `lib/weather.ts:7-16,147` and `components/MonteCarlo.tsx:84-117`. The keyless-browser-API constraint the note worried about turned out to be satisfiable: Open-Meteo needs no key and answers CORS, so nothing here was ever blocked. **The remaining difference is honest and worth naming rather than deleting:** RocketPy disperses over an ENSEMBLE or a reanalysis archive, so its spread carries forecast uncertainty; Loft disperses over one deterministic profile with a synthetic spread around it. That is a narrower gap than this row claimed and belongs on its own row when someone scopes it. |
| 9 | RocketPy | **Full 6-DOF rigid body dynamics** | Loft's state is 6-DOF-*ready*; the solver is not yet 6-DOF | `QUEUED` | Named in the North Star. Belongs on the roadmap explicitly rather than as an aspiration — decompose when the fundamentals justify it. |
| 10 | RocketPy | **Sensitivity analysis** — which input drives the outcome | Parameter sweep exists (`components/ParameterSweep.tsx`); ranked sensitivity does not | `GAP` | Cheap on top of the existing sweep, and it is the question a flyer actually has. |
| 11 | RASAero II | **Fin flutter prediction** | **Shipped, with the method cited and the envelope stated** — `lib/sim/flutter.ts` implements the simplified flutter-boundary closed form derived from NACA TN 4197 (Martin, 1958), judges each fin set over the flight its own stage was attached for, and its shear moduli each carry a source. Six materials publish no shear modulus and the table SAYS so rather than inventing one | `HAVE` | **RESOLVED 2026-08-03 — this row said "Nothing" and had been stale for several runs.** The note's own condition was met on the way in: the method is cited, the envelope is stated, and R8 increment 1 went back and re-derived every modulus from a primary source (USDA Wood Handbook FPL-GTR-282, MIL-HDBK-5J), correcting basswood by a factor of three and balsa by 1.5x. A stale `GAP` is worse than no row: it invites a future run to build what already exists, which is exactly what the roadmap's own queue is meant to prevent. |
| 12 | RASAero II | **Validated supersonic drag** | Loft's drag model — see `/docs/limitations` | `GAP` | `UNVERIFIED` how large the gap is. Measure ours against RASAero output on a corpus design before scoping; the measurement is the increment. |
| 13 | RockSim / OpenRocket | **Unit preference honoured everywhere**, set once | Units control exists | `HAVE` | Confirm on every new surface — this is the kind of thing that regresses one component at a time. |
| 14 | All desktop tools | **The design is a file the flyer owns**, saved and reopened losslessly | `.ork` export exists and is known to drop `ballastKg` | `QUEUED` | R6. A builder whose output loses parts is worse than no export. |
| 15 | ThrustCurve | **Complete, current motor data** including updates | Bundled offline catalogue (`lib/motors/catalog.ts`) | `GAP` | Offline-first is correct and is a real advantage. The gap is staleness: nothing tells a flyer how old the catalogue is or refreshes it. Say the date at minimum. |
| 16 | OpenRocket, RockSim | **3D / photo-realistic view of the design** | 2D diagram (`components/RocketDiagram.tsx`) | `REJECT` for now | The 2D diagram is the direct-manipulation surface and it is not finished. 3D is decoration until the editor is complete; revisit after the P-track. |
| 17 | All | **Runs offline, installs, costs nothing, needs no account** | PWA, fully client-side, MIT | `BETTER` | Every desktop competitor is an install; RocketPy needs Python. This is Loft's genuine structural advantage and it is under-sold on the landing surface. |
| 18 | All | **Reads several design formats into one model** — `.ork`, `.rkt`, RASAero | `lib/ork`, `lib/rkt`, `lib/rasaero` | `BETTER` | **The note said "No competitor reads its rivals' files" and that is REFUTED, 2026-08-02.** OpenRocket opens and exports RockSim `.rkt` (its File menu offers *Export as… Rocksim 10 (.rkt)*), and the RASAero II Users Manual v1.0.2.0 documents importing RockSim files under *File → Import → RockSim*. Cross-reading is not the differentiator; **reading three formats into ONE internal model and then showing each file's own stored results beside Loft's** is — no competitor presents a rival tool's predictions next to its own (row 19). Narrowed rather than dropped. Under-sold either way: a flyer does not know this until they try. |
| 19 | All | **Shows another tool's stored results beside its own as a cross-check** | Import carries stored results through; `components/RocketpyCrossCheck.tsx`, `components/DragCrossCheck.tsx` | `BETTER`, and the claim under it is narrowed | **CORRECTED 2026-08-05: "nothing else in the field does it" is no longer true, and it is worth being exact about what changed.** RocketPy **1.12.0** (2026-03-08) shipped `FlightComparator`, documented to compare a `Flight` against real flight data (avionics logs, altimeter CSVs, GPS) *and* against other simulators' output — and Loft pins RocketPy 1.12.1, so it is the version already in the bundle. Marked `UNVERIFIED` for the exact API surface, which was read from release notes rather than exercised. **What survives, and it is still a genuine lead**: RocketPy's comparator is a Python object a user writes a script around, and OpenRocket, RockSim and RASAero II still have nothing of the kind. Loft's is a WORKSPACE — a flyer imports a file and the file's own stored numbers, a second solver, and Loft's own result are all on screen without writing anything. The differentiator is the affordance rather than the capability, and that is a weaker claim than this row used to make. **And the row understated one thing while overstating another**: RocketPy's headline accuracy is validated against FLOWN rockets (~1% apogee on three university flights, JAE 2021) and RASAero II publishes a 41-flight measured-vs-predicted table with an average error of 3.47%. Loft's census oracle is another simulator's PREDICTION on 26 of 38 corpus files, and the corpus already carries 4 rows with an actual flown result that nothing compares against. That gap is filed and is the strongest single candidate for the next validation milestone. **The "wants to become a first-class view" half of this row is RESOLVED as of 2026-08-02**: it is `/validate`, its own route with its own title, holding all three of the cross-checks — the design file's own stored numbers, its step-by-step flight overlaid on Loft's, and RocketPy run in the browser. Two of those three were in the Flight panel and the third was in Analyze, so until P2's second increment the independent estimates North Star #1 asks to be shown side by side were on different routes. What is still owed is the PRESENTATION rule §6 states — several solvers' results as independent estimates that can disagree, never averaged — which is R9's job now that it has a view to live in. |
| 20 | OpenRocket, RockSim | **A move that cannot be made is refused, and a move that can is re-checked.** Drag-drop reorder in the component tree has shipped in OpenRocket since **1.1.3** — the only release whose notes carry the bullet; this row claimed 12.09 and 22.02 re-listed it until 2026-07-31 and they do not. **22.02.RC.01** added *"additional warnings for inline pods, gaps and overlaps in airframe"* (the RC heading, not 22.02 final). RockSim relocates a dragged part only *"if you dropped the component onto a valid location"* — its Program Guide states no other outcome, so a refused drop is a silent snap-back | **R4 shipped 2026-07-31**: `moveSlots` + `GeometryEdits.moved`, a drag on the diagram with a drop indicator, a button pair as the keyboard and touch path, and a stage-boundary rule that leaves the illegal drop OFF the offer rather than showing it and refusing. 484 drop slots and 206 reorders driven across all 35 real designs. `lib/validation/` still carries no gap or overlap rule | `HAVE` | Resolved for the mechanic; the SECOND half of the row stands. Verified from OpenRocket's `ReleaseNotes.md`, its wiki Tips page and the readthedocs getting-started layout, and from the Apogee RockSim Program Guide (v8-era, so `UNVERIFIED` for v10/v11). **What Loft does that neither documents:** an illegal drop is not offered at all, so there is nothing to refuse — and a reorder that produces an airframe the model cannot fly says so, which is row 22. **What OpenRocket still has that Loft does not:** an explicit gap/overlap warning on the airframe. R4's *done when* asserts no overlap and the corpus sweep holds it, but nothing warns a flyer about one. |
| 21 | OpenRocket | **One look-and-feel engine driving every surface, with density as a user-adjustable global.** **23.09** shipped dark mode (normal and high-contrast) and custom UI font size — this row said 22.02 until 2026-07-31, which is wrong: `ReleaseNotes.md` puts that bullet under 23.09, and 22.02's UI bullets are show/hide warnings, multi-component edit and the motor/sim table rework. 24.12 moved every theme onto one engine and added UI Scale, Font Size and Character Spacing, and per-component show/hide on the design view (*"Allow components to be hidden from view"*, verbatim under 24.12). A separate design-panel text size was claimed here on 2026-07-31 and removed the same day: `ReleaseNotes.md` has no such string, and it is `UNVERIFIED` whether the preferences UI carries one | `components/ui.tsx` plus `lib/ui-tokens.ts` are the token layer and P1 is converting onto them; **14 of 23** components import from them, and only **3** import nothing but `Card` | `QUEUED` | P1, then P2. Verified from `ReleaseNotes.md`. The bar a mature hobby sim sets is not "consistent-looking" — it is one engine, tweakable globally, which is exactly P1's "extraction, not a repaint". It also says the tokens must be a single source rather than per-component class strings. RockSim has no documented equivalent (`UNVERIFIED`). |
| 22 | OpenRocket, RASAero II, RocketPy | **A number the model cannot stand behind is flagged, not just computed.** OpenRocket 22.02 added *"additional warnings for inline pods, gaps and overlaps in airframe"* and ships a *"Discontinuity in rocket body diameter"* warning; RASAero II offers no reorder at all (right-click gives Edit / Insert / Delete, and an insert lands *"ahead of the original component which the right-hand mouse button was clicked on"*, Users Manual pp. 36–37); RocketPy has no ordering concept at all — every surface is placed by an absolute `position=` on `add_nose`, `add_trapezoidal_fins` and `add_tail`, with no documented gap or overlap check | **The discontinuity half is RESOLVED 2026-08-02.** Loft named a diameter step only behind the part a flyer had PICKED (`mouldLineStep`), so an imported design carried one silently — 9 of the 35 corpus designs arrive stepped above the 0.5 mm notice threshold (27 joints, median 12.70 mm, up to 82.55 mm) and none of them said a word until the flight itself did. `mouldLineSteps` now walks the whole airframe and the FLIGHT cautions, names the step and says the numbers read optimistically — which is the shape of OpenRocket's own *"Discontinuity in rocket body diameter"*. As of 2026-07-31 a flight whose airframe leads with a flat face says so too. It still has no gap/overlap rule | `BETTER` on the blunt face, `HAVE` on the diameter discontinuity, `GAP` on gaps and overlaps | The measurement that earned the row: on `fixtures/demo-quirks.ork`, nudging the nose one place aft leaves apogee 1406.622 m, max velocity 227.893 m/s and rail exit 26.023 m/s **every digit identical** while the rocket flies a 66 mm flat disc — because forebody drag is taken from the nose cone wherever it sits and there is no term for a flat face. No competitor documents a warning for that specific shape, and two of the three cannot reach it because they cannot reorder. 0 of the 35 corpus designs fire it as imported. **Where Loft now goes past OpenRocket on the discontinuity:** OpenRocket warns that the diameter is discontinuous; Loft additionally states WHY it matters to the number on screen — it charges a transition by its joint angle and has no term for a bare step, so it names the drag as under-counted rather than only naming the geometry. What it deliberately does NOT do is put a figure on the missing drag: eq. 3.86 at its abrupt limit (0.8·ΔA) takes `02.Two-stage.ork` from agreeing to 35.2% low, because 0.8 is Hoerner's flat-face value in clean air and a step is an annulus inside a boundary layer. The published forward-facing-step coefficient as a function of step height over boundary-layer thickness is the source that would close it, and it is `UNVERIFIED` whether one exists in a citable form. Verified from the RASAero II Users Manual (the quotes are verbatim; the page numbers are `UNVERIFIED`), docs.rocketpy.org/user/positions — which documents the per-surface `position=` above rather than the batch signature this row first cited — and OpenRocket's advanced-design page. |
| 23 | OpenRocket | **The design view IS the window; the numbers live in a separate dialog.** The main window is the component tree (top-left), add buttons (top-right) and the rocket figure filling the bottom half — every dimension is edited in a Component Configuration window that opens over it, so no permanent pixel of the design surface is a number field | **P2 increments 1-5, 2026-08-02.** Five distinct static routes — import at `/`, then `/flight`, `/design`, `/sweep`, `/validate` — behind one navigation spine, with the design and its results surviving a move between them (they are mounted in the route-group layout, which Next does not remount). The numbers are no longer all on one viewport: the design summary keeps three headline figures above the spine and folds the other seven behind a control on a phone, which took 157 px out of the shared chrome on all four routes — though `/sweep` is still 82 px past the two-screen contract once the pointer is measured as coarse, and the fine-pointer measurement that briefly said otherwise is corrected in `ROADMAP.md`. What is NOT matched is the *dialog*: Loft's editors are still inline on `/design` rather than opening over the drawing | `HAVE` on the route split, `GAP` on getting the fields off the drawing | This row was the measurement P2's argument was missing, and P2 has now answered the half it could. Verified from openrocket.readthedocs.io/setup/getting_started and wiki.openrocket.info/Basic_Rocket_Design. It is a *dialog*, not a tab — the route split is the web-native form of the same decision. **The residue is worth stating precisely**, because it is the next P-track argument: OpenRocket's design view has NO permanent pixel of number field, so the drawing gets the whole window and a dimension is edited in a window that opens over it. Loft's `/design` still carries its numeric fields inline beside the diagram, so the drawing never gets the full width. A modal editor is not obviously the right web answer — it costs a click and breaks direct manipulation — but *something* has to give the diagram the room, and P4 (a touch-native builder) is where that decision belongs. |
| 24 | RockSim | **The flyer chooses which columns a results table shows, and how verbose the labels are.** *"Available simulation summary columns"* / *"Displayed simulation summary columns"* with Add/Remove, plus *"Use long column names on the simulation summary screen… de-selecting this option displays the simulation list with abbreviated column labels"* | **Resolved for affordance PARITY on 2026-08-01, still `GAP` for column choice.** All six tables are now one primitive (`components/DataTable.tsx`), so sort, `aria-sort`, a sticky header, copy and CSV export are one implementation rather than the five partial ones this row measured. What RockSim still has and Loft does not is the row's actual subject: the flyer choosing WHICH columns show, and how verbose the labels are | `GAP` | The only tool in the field that makes table density a user setting, and a cheap, on-brand answer to `DESIGN.md` §4's "density is the point" — which today fixes density for everyone. Sizes the `DataTable` primitive P1 already names as its last slice: sort, copy and column choice are one implementation where there are now five partial ones. Verified from the Apogee RockSim Program Guide's Preferences section (v8-era, `UNVERIFIED` for v10/v11). |
| 25 | OpenRocket, RockSim, RASAero II, RocketPy | **A staged flight presented as a table of what HAPPENED — one row per event or phase, not one row per simulation.** None of the four has one. OpenRocket's results grid is one row per SIM across 11 fixed scalar columns (`Name, Configuration, Velocity off rod, Apogee, Velocity at depl…, Optimum delay, Max. velocity, Max. acceleration, Time to apogee, Flight time, Ground hit velo…`), and its flight events are plot markers and CSV comment lines only — *"you can choose from several flight events, any or all of which can be called out on your plot"* — over five event types (Motor ignition, Motor burnout, Apogee, Recovery device deployment, Ground hit), **separation not among them**. RockSim's table is also one row per simulation. RASAero II is the only one that names a phase in a grid, and it is per-TIMESTEP, not per-event: columns 2 and 3 are `Stage` and `Stage Time (sec)`. RocketPy prints no table at all — `events_registered` covers parachutes only, and staging is `initial_solution=` chaining of separate `Flight` objects | The solver already emits 8 event types and **239 events across the 35 corpus designs** (deploy 40, ignition 35, liftoff/rail-exit/burnout/apogee/landing 31 each, separation 9), and `StagePhase[] {startTime, stageCount}` is already built and time-ordered — but it is **thrown away**: neither `FlightRun` nor `FlightResult` carries it, and there is no phase table on any surface. Separation reaches the altitude chart as a marker and is filtered OUT of `FlightViz`'s event dots entirely | `GAP`, and an available `BETTER` | **QUEUED as R5 increment 2**, which is what R5's *done when* — "a staged flight whose phase table matches what they built" — has been asking for. The row is worth more than the feature: this is not catch-up, it is a surface no competitor offers, and the data is already computed. Two things to take from them rather than invent: RASAero's **stage-elapsed time beside flight time** (not currently derivable — it would be new), and OpenRocket's per-stage data branches (23.09: *"The simulation engine will create new FlightDataBranches for each stage after separation… the user selects which stage's data to show"*), which is the larger staging gap behind the table. Verified from `wiki.openrocket.info` SimResults, openrocket.readthedocs.io/user_guide/advanced_flight_simulation, OpenRocket `ReleaseNotes.md`, the RASAero II Users Manual figs 125–126, the Apogee RockSim Program Guide, and docs.rocketpy.org flight_prints. **Row 24's claim that OpenRocket's simulation table sorts on a header click was REFUTED while checking this** — that wiki sentence is about the motor-selection list; sim-table sorting is `UNVERIFIED`. Filed in `BACKLOG.md`. **Two claims in this row were themselves corrected on 2026-07-31, both against `ReleaseNotes.md` on `unstable`, and both in the direction that makes Loft's position weaker rather than stronger — which is the direction a competitive ledger gets wrong by default.** (1) The per-stage `FlightDataBranches` and stage-selector quotes are dated 23.09 above; the file puts **both bullets under 13.05 (2013-05-04)**, a decade earlier — so per-stage data branching is a settled baseline in this field, not a recent move, and the staging gap is correspondingly older than this row implies. (2) "No competitor exports its results grid" is refuted outright: 23.09 carries *"Export sim table to CSV (fixes #2077)"*, and issue #2077 is closed. Row 26 carries the consequence. |

| 26 | OpenRocket | **A results table is a keyboard surface with an exit, not a rendering.** `ReleaseNotes.md` 22.02.beta.05 ships three bullets that are really one contract — *"Use tab and arrow keys to traverse sim table (fixes #1552)"*, *"Ctrl/Cmd+A (select all) keyboard shortcut in component tree, simulation and motor configuration tables (fixes #1549)"* and *"Restore focus to motors, recovery, stage and simulation tables after table action and others (fixes #1558)"*: arrow-key traversal, select-all, and focus returned after the action. 23.09 adds the exit — *"Export sim table to CSV (fixes #2077)"* | **`DataTable` shipped 2026-07-31** (`components/DataTable.tsx`), and the three tables that offered NOTHING are on it — `ValidationPanel`, `RocketpyCrossCheck` and the phase table. Each now sorts on any column that offers a value, with `aria-sort` on the header and a real `<button>` inside it, keeps a **sticky header** (which not one hand-rolled table had), and copies and exports to CSV. Walked in the built export: `aria-sort` cycles `none → ascending → descending`, the row order reverses, both export controls render. **All six are now on it** — `MassBreakdown` (2026-07-31), then `MotorSweep` and `GeometryInspector` on 2026-08-01, which needed four things the primitive could not express: a controlled `sort`/`onSortChange` pair, a per-column first direction, `rowProps` for a row that is itself the pick target, and a comparator that survives a non-finite value (`(x - y) * dir` returns NaN, which makes a comparator INVALID and scrambles the whole array, not just that row). The parts table gained copy and CSV export it had never had, closing this row's export clause for the last surface that offered nothing. **Still absent: filter, select-all, arrow-key traversal within the grid, and focus restoration.** Previously: six bespoke elements and no primitive. `GeometryInspector.tsx:559` sorts and puts `tabIndex={0}` on **every** row, so Tab walks all N with no arrow keys; `MotorSweep.tsx:342` sorts and copies; `MassBreakdown.tsx:81` copies; and `ValidationPanel.tsx:102`, `RocketpyCrossCheck.tsx:346` and the new `PhaseTable` (`ResultsView.tsx`) offer **nothing** — no sort, no copy, no export. No table anywhere has a sticky header, a filter, select-all, or focus restoration, though `ui.tsx` already implements roving tabindex for `Tabs` and already exports `useReturnFocus` | `GAP`, narrowing | Sizes the **keyboard and export** half of `DataTable` where row 24 sizes the **column-choice** half; together they are the whole primitive, and it is P1's last slice. Verified from the project's own `ReleaseNotes.md` on `unstable` and from github.com/openrocket/openrocket/issues/2077 (closed). **What Loft still leads on, and is under-selling:** `FlightViz.tsx:68` marks six event types against OpenRocket's five documented plot callouts — separation is not among theirs — and RocketPy's `events_registered()` covers parachutes only (docs.rocketpy.org `flight_prints`), because its staged flights are separate `Flight` objects chained by `initial_solution=` and nothing prints across the join. That is the structural reason no competitor can match a whole-stack phase table, and row 25 should say it rather than only "none has one". **The cheapest thing to take is the export**, because `PhaseTable` is the row-25 differentiator and today its numbers cannot leave the page at all. |

| 27 | OpenRocket, RockSim | **A cluster is authored on the mount you picked, and the tool builds the extra tubes as real parts.** OpenRocket clusters only an inner tube — *"Clustered tubes (which are, by OpenRocket's definition always Inner Tubes)"* — from a **Cluster tab** on that one component, and replicates every attachment across the cluster (*"all of the tubes in the cluster will receive one"*). RockSim's per-tube Cluster wizard *"makes n copies of the tube"*. So in both, the extra motor tubes ARE components: their mass, their centring rings and their attachments all follow from the tree rather than from a multiplier | Loft has **one global "Motor cluster" field** (`LoftApp.tsx`) with no way to pick which mount it acts on. As of 2026-08-01 it writes only the mounts its own value truthfully describes and states how many those are — but that is still a group, not a pick. `lib/sim/mass.ts` scales an inner tube's own mass by the count and adds nothing at all for an airframe-hosted cluster, because the design describes no tubes there | `GAP` | **The mass Sev-1 fixed on 2026-08-01 converged on their structural rule from the other side.** Neither tool has an airframe-hosted cluster at all, which is exactly why neither ever multiplies an airframe — the case cannot arise for them. Loft still OFFERS the field on the 12 corpus designs whose mount is a body tube, so it can reach a state neither competitor models. Two things to take, in order: **a mount picker** (RockSim's engine load-out is a per-mount table — *"select the motor mount by clicking on it's row… You may select more than one motor mount at a time"*), and then **real tubes** rather than a multiplier. Verified from the OpenRocket wiki's clustering page and the Apogee RockSim Program Guide (v8-era, so `UNVERIFIED` for v10/v11). |
| 28 | OpenRocket, RockSim | **A stage's ignition and separation are settable per stage and per configuration, not derived.** OpenRocket adds a stage from the *Assembly Components* palette — *"components that have no physical meaning of their own but are used to group components together"* — and its ignition event and delay are edited per stage per motor configuration. RockSim refuses to simulate a stage carrying no motor mount at all: *"you will not be able to run flight simulations on your design"* | Loft **derives** both from the stage index (`lib/sim/setup.ts`): the bottom stage lights at launch and each stage above air-starts at the burnout below it, plus any delay the FILE records. An authored booster inherits the seed's configuration entry with `ignitionEvent` and `ignitionDelay` deliberately omitted, and a flyer cannot set either | `GAP` | Sizes what R5's *done when* does NOT cover and what an R5 successor should. The derivation is right as a DEFAULT — 10 of the corpus's 12 real boosters reduce to it — and wrong as the only option. **What Loft does that neither documents:** it refuses the booster gesture up front where the aft tube carries no mount, rather than letting the design exist and failing at simulation time (RockSim) or at run time (OpenRocket's *"No motors defined"* abort). Verified from openrocket.readthedocs.io's component reference and the Apogee RockSim Program Guide (`UNVERIFIED` for v10/v11). |
| 29 | OpenRocket, RockSim | **A cluster that cannot fit is refused, and the refusal states the modelling reason.** RockSim's wizard refuses outright: *"WARNING: The cluster as specified will not fit in the airframe. You are not allowed to create clusters that are outside the airframe."* | Loft bounds the Motor cluster field at 1–12 and checks nothing about whether that many motors of that casing fit inside the tube they are mounted in | `GAP` | Cheap and squarely inside the SAFETY posture — an input that cannot mean anything physically should be refused or bounded rather than flown into a confident number, and 12 × 98 mm inside a 98 mm airframe is exactly that. The geometry is already available: the mount's host carries an outer radius and the resolved motor carries a casing diameter. `UNVERIFIED` for RockSim v10/v11; the quote is from the v8-era Program Guide. |
| 30 | OpenRocket, RockSim, RASAero II | **Saving your work is lossless, because the file a tool writes is the file it reads.** Each of the three owns its design format — OpenRocket `.ork`, RockSim `.rkt`, RASAero II `.CDX1` — so a save-and-reopen is a fidelity no-op by construction, and none of them has to have a policy about it | **Loft's own save is a FOREIGN-format export, and it is not yet a fixpoint.** `downloadOrk` writes `.ork` — the format Loft ingests, not one it defines — so every save is a translation, and a design saved and reopened is not the design that was saved. Measured 2026-08-02 across 35 corpus designs plus the authored starter: **0 of 36** reach a byte-equivalent internal model on the first export → re-import (min 11 non-id field diffs, on `A simple model rocket.ork`; max 146, on `FullScaleModelTH.rkt`; the authored design 33). The model becomes a fixpoint only on the SECOND trip. Named losses, each measured: freeform fin sets written back as trapezoids (9 sets on 8 designs — `Pods--airframes and winglets.ork` 2.134 → 1.449 cal static margin, −32%, and apogee 193.64 → 188.45 m; `rocksimTestRocket2` loses its `over-stable` warning outright); per-configuration ignition collapsed to one delay (all 5 of `Airstart timing.ork`'s configurations become the identical 1296.52 m, from a 1268.50–1296.52 m spread); motor-configuration NAMES dropped (29 configs on 9 designs — "Airstart @2s" comes back as a UUID, and the starter's "H128W" as `cfg-1`); `plugged` dropped on 42 motor instances, where a plugged motor's NaN delay is written as `<delay>0</delay>` — "carries no ejection charge" becomes "fires at burnout"; and canopies given an invented `<overridemass>` (24 canopies on 18 designs) which then defeats a later resize on 14 of 27 designs | **`PARITY` for a design authored in Loft, `GAP` narrowing for an imported one** | **R6 SHIPPED 2026-08-02, and this row is the measurement its *done when* was asserting without.** A design built in Loft now round-trips part for part and id for id — 0 of 9 components change identity, where 3 did — asserted by a test that names what a trip through the file may change rather than comparing loosely. So on the thing the three desktop tools get free, Loft is now level: your own work survives being saved. Four losses that hit IMPORTED designs were closed on the way (per-configuration ignition, configuration names, freeform outlines, plugged motors), and the rest stay inventoried below. The asymmetry is structural and worth stating plainly: the three desktop tools get losslessness free because they define their formats, and Loft has chosen a harder position — its save is portable to OpenRocket, which none of theirs is to each other. That is the trade R6 has to make honest: either retain the field or name it as a documented exception, and pin both with a test. The competitor column is `UNVERIFIED` as a *claim about their implementations* — it is inference from the fact that each reads and writes one format it owns, not from a documented round-trip guarantee; verifying it means a save-reopen-diff on each tool, which needs the tools. What is NOT inference is the Loft column: every number in it is from a run over the corpus. **One Sev-1 already fixed out of this row on 2026-08-02** — a RASAero design's stated launch weight came back deletable, so a flyer could take a 453.6 g rocket to 0.0 g and still be given a confident apogee. |

| 31 | OpenRocket, RockSim, RASAero II | **Switching workspace changes the panel, not the subject — the design stays on screen.** OpenRocket's window is *"divided horizontally into four sections"*: Main Menu, Task Tabs (*Rocket Design*, *Motors & Configuration*, *Flight Simulations*), the tab's Pane, and the **Rocket Views Pane**, which is its own section BELOW the tabs — the Top/Side/Back and 3D views are on screen while you pick a motor and while you run a simulation, and the tab swaps only the pane above them. RockSim keeps views of the design beside its five tabs (`UNVERIFIED` whether they persist across tabs; the guide read is v8-era). RASAero II draws the rocket on the input screen itself — *"a scale rocket drawing is created on the Main Screen. The rocket Center of Pressure (CP) of each stage will be displayed"* | The route split mounts the drawing in **one** workspace: `RocketDiagram` is reachable only through `GeometryInspector` (`components/GeometryInspector.tsx:391`), which renders only inside `panel-design` (the `#panel-design` region in `components/ResultsView.tsx`). What survives a move to Flight or Analyze is the summary strip and the flight warnings — numbers, not the picture | `GAP` | **Names the one thing P2 can COST that the page it replaces did not:** a flyer sweeping a fin, or reading a dispersion, loses sight of the airframe both are about. On the scrolling page the drawing was one scroll away from everything. The cheap answer is a compact persistent design strip above the spine. **Corrected 2026-08-01:** the reasoning about remounting is right, the file is wrong. `app/(app)/layout.tsx` renders `<LoftApp>{children}</LoftApp>`, so everything under `LoftApp` already survives navigation identically — the strip's insertion point is `components/ResultsView.tsx:444`, immediately above `<WorkspaceNav />`, where all six props it needs (`shownRocket`, `units`, `cgLoaded`, `stability.cp`, `staticMarginCal`, `shownMotors`) are already in scope. Zero prop plumbing, zero new state. Two constraints found by driving it: the strip must sit OUTSIDE every `hidden` region (`useMeasuredWidth` reads 0 inside one) and must be suppressed while `tab === "design"` or the drawing appears twice, which is also what makes four existing e2e selectors ambiguous. **Not a reopening of row 16** — this is the persistence of the 2D drawing, not 3D fidelity. Filed as a P2 slice. **RESOLVED 2026-08-10 on desktop and tablet, DELIBERATELY NOT on a phone.** `components/AirframeStrip.tsx` renders the drawing above the workspace spine, outside every `hidden` region, on every route but `/design` — where the full drawing is already the top of the workspace and a second copy would make the same accessible name match twice. It is read-only by construction: no `onEdit`, no `onMoveTo`, no hover or select, because the editing affordances belong to the surface that owns them. It carries the CG and CP marks and the static margin, gated on `run.motorsComplete` exactly as the full diagram's caller is, and is registered in `lib/margin-surfaces.test.ts` for it. **Two measurements decided the shape.** A horizontal drawing is true to scale with its length filling the column, so its HEIGHT is set by the fin span — the same drawing that is 12 px tall in a narrow panel is **216 px** across a full-width desktop column, which put 280 px of chrome above every workspace. `RocketDiagram` therefore gained one prop, `variant="strip"`: a 72 px height budget (the rotated layout's own pattern), no rotation on a phone held upright, and no zoom control. **And the phone is the honest half.** Even at 72 px the strip pushed the sweep's first answer to **2.13 screens** at 390x664, past `DESIGN.md`'s two-screen rule, so it is `hidden sm:block` and a phone's chrome is byte-for-byte what it was. Every tool this row cites keeps its rocket view in a desktop window; on a phone the drawing stays one tap away. `e2e/depth.spec.ts`'s desktop cap moved 820 → 920 with the reason recorded in the file, and the phone cap did not move at all — so a future change that renders the strip below `sm` goes red there before it breaks the two-screen rule. |
| 32 | OpenRocket, RockSim, RASAero II | **Nobody else has a phone at all.** OpenRocket is a desktop Java application, RockSim a desktop Windows/macOS product, and RASAero II is Windows-only — there is no phone build of any of the three, so a flyer standing at the pad with a rocket on the rail cannot open their design in ANY of them. The comparison here is not against a competitor surface; it is against the absence of one | **Loft runs on the phone, and holds itself to a contract the others never have to meet.** `DESIGN.md` §8: *a phone journey is at most two screens deep to its answer*. Measured 2026-08-01 at 390x664 on the built export with the bundled sample, and now pinned by `e2e/depth.spec.ts`: `/flight` **1.53** screens to the apogee tile, `/design` **1.55** to the airframe drawing, `/validate` **1.70** to the stored-tool comparison — and `/sweep` **2.10** to the first swept-motor row, which is a real breach. The cause is one shared term: **1071 px of chrome above the workspace spine**, identical on all four routes, which is 1.61 of the two screens before a workspace renders a pixel. The design summary is 508 px of it, against 316 px on desktop | `HAVE`, with one route breaching | **The verdict is `HAVE` because the capability is uncontested — a pad check on a phone is something none of the three can do at all — and the breach is against Loft's own standard, which is the only reason it is measurable.** That is the honest shape of this row: the bar here is self-imposed, so the risk is not losing to a competitor but quietly dropping the bar. Hence the pin: `/sweep` is a `test.fail`, so it runs, measures, and goes red the day it is fixed, rather than a widened threshold that would never have spoken. The chrome is ratcheted separately because it is the one number every route's depth is built on. **UPDATED 2026-08-02, and every figure above it is superseded.** The breach is CLOSED: `/sweep` measures **1.90 screens**, 68 px inside the contract. Two changes did it — the design summary's reference figures folded behind a phone-only control (157 px off the shared chrome on all four routes at once) and the sweep's own explanatory paragraph now shows only until the sweep has run (140 px, and after it runs the table answers the same question the prose was answering). **And the measurement itself was wrong.** The phone context was a phone-sized viewport over `devices["Desktop Chrome"]`, so it reported `pointer: fine` and rendered every `TOUCH_TARGET` control at 26 px instead of 44 — understating the chrome by **97 px**, in the direction that makes the app look like it passes. The spec sets `hasTouch` now and the ratchet is ≤820 px desktop / **≤1060 px** phone. A contract `DESIGN.md` writes for touch has to be measured on touch, and that is the lesson worth more than the pixels. **Sequencing note that binds row 31:** the persistent design strip still costs a phone 130–160 px, which is more than the 68 px of headroom now in hand, so it remains a separate increment with its own budget rather than a rider. |
| 33 | OpenRocket, RockSim, RASAero II, RocketPy | **A number outside the envelope its method was validated over is marked ON THE NUMBER, not in a message elsewhere.** **Not one of the four does this** — every one of them, where it says anything at all, says it in a separate place. OpenRocket comes closest and it is worth being precise about: `Warning.SUPERSONIC` ("Body calculations may not be entirely accurate at supersonic speeds") is a genuine MODEL-VALIDITY warning, fired from `SymmetricComponentCalc` at **Mach > 1.1** — so nothing at all fires in the transonic band 0.8-1.1, where the drag rise actually happens. It lives in a per-SIMULATION `WarningSet` surfaced in the simulation table's Warnings column, undifferentiated from design warnings like `DISCONTINUITY` or `NO_RECOVERY_DEVICE`, so the UI never distinguishes "your rocket is wrong" from "my method is out of its range". **RASAero II has no out-of-envelope concept at all** — its manual claims validity Mach 0.01-25 and the word "extrapolat" does not appear in it; its only in-software warnings are modal stability dialogs. **RockSim** documents its Mach-0.8 transonic correction as prose on the Drag Analysis screen — a description of a model, not a caveat on a result (and it is the same 0.8 threshold Loft uses). **RocketPy extrapolates SILENTLY**: `Function` extends a user-supplied Cd-vs-Mach curve past its last data point with no signal, none of the 7 `warnings.warn` sites in `mathutils/function.py` fires on evaluation outside the domain, and `Flight.apogee` is a plain float with no validity attribute | Seven ascent-derived readouts carry an `extrapolated` marker with the reason and the range, on the readout itself, from `DESIGN.md` §5's required treatment; rail-exit velocity and thrust-to-weight deliberately do not, being inside the envelope whatever the flight does later. Pinned both ways in `e2e/smoke.spec.ts` — a transonic flight marks, a subsonic one does not | `BETTER` | Added 2026-08-02, and this is a genuine differentiator rather than catch-up: the marker is on the value, at the moment of reading, which is the only place a flyer who is not reading the warning card will see it. **Two claims were checked and one was refuted**: OpenRocket's warnings are NOT on the plot — `getWarningSet()` has exactly one consumer in the entire Swing tree — and its supersonic trigger is 1.1, not the 0.8 a drag-rise caveat would want. **What to take from outside the field:** CF conventions bind a `status_flag` variable to the value it qualifies via `ancillary_variables`, with enumerated `flag_values`/`flag_meanings` rather than free text; QARTOD uses an ordinal scale (1 Good, 3 Suspect, 4 Fail); NIST REFPROP returns range-of-validity warning codes; ASME V&V 10/20 calls the concept the "validation domain". Loft's marker is close to CF's shape already — the improvement worth making is an enumerated severity and enumerated reasons rather than a prose string, which is filed in `BACKLOG.md`. Verified from OpenRocket's own source and `messages.properties`, the RASAero II Users Manual, the Apogee RockSim Program Guide (v8-era, so `UNVERIFIED` for v10/v11) and RocketPy's `mathutils/function.py`. |
| 34 | OpenRocket, RockSim, RASAero II | **What "ground hit velocity" MEANS — and nobody states it, so it has to be measured.** Every `.ork` and `.rkt` stores the figure and none of the three documents whether it is the vertical descent rate or the speed over the ground including wind drift. Established here empirically rather than from documentation, which is why it counts: `USLI2025-FULLSCALE-10.15` carries five OpenRocket runs at 0, 5, 10, 15 and 20 mph of wind, and its stored `groundhitvelocity` is **5.609 / 5.607 / 5.608 / 5.610 / 5.607** — flat to four significant figures — while the same runs' stored flight time and altitude both move. A figure that does not respond to wind is not measuring a speed over the ground. RockSim's `VelocityAtLanding` behaves the same way against Loft's vertical figure across the corpus. **RESOLVED FROM SOURCE 2026-08-04, and the inference was right for one era and wrong for the other.** OpenRocket interpolates the stored attribute out of `TYPE_VELOCITY_TOTAL` at its `GROUND_HIT` event — logic byte-identical across every release — but what that series HOLDS during descent changed at **24.12**. Read directly: in `release-23.09`, `AbstractEulerStepper.java:168` is `data.setValue(TYPE_VELOCITY_TOTAL, airSpeed.length())` with `airSpeed = status.getRocketVelocity().add(windSpeed)` — the AIR-relative speed, which under an open canopy (where the rocket drifts *with* the air) is effectively the vertical descent rate, exactly as the flat-across-wind inference concluded. In `release-24.12` that stepper contains **zero** references to the type, and `SimulationStatus.java:643` sets it from `getRocketVelocity().length()` — the GROUND-frame total, drift included, the same quantity RockSim stores | **Loft reported the total speed over the ground and called it the same thing.** Under an open canopy the horizontal component simply IS the wind, so on that USLI design Loft read **10.46 m/s against the file's 5.607 at 20 mph**, and the landing energy built on it was **801 J against 215 J**. It now reports the vertical descent rate under that name — median disagreement with the corpus's stored figures **0.68% on the nine sims where wind exceeds 4 m/s**, against 25.27% for the total — and carries the total separately as *Arrival speed* when drift makes it materially larger | `HAVE`, and the gap it exposed turned out to be mostly the comparison rather than the model | Added 2026-08-02. **The reason this is a benchmark and not a bug report:** the convention was never written down by anyone, so Loft matched it by accident for years and then diverged silently. The published accuracy figure for this metric went **3.0% → 8.3%** as a result, and that is the honest direction — the old number was two errors cancelling, because Loft's own descent rate runs low on the openrocket files and the wind term ran high (`pods--airframes and winglets` reads −14.5% vertical but −3.0% total). **What Loft does that none of the three do:** report both, separately labelled, rather than one number that quietly means whichever the author had in mind — and now, read each stored figure as the quantity its own writer's version actually stored. **Resolved 2026-08-04 by R10 increments 1–2.** The corpus splits 27 stored simulations written by OpenRocket 23.09 or earlier against 64 by 24.12 or later, so most of the OpenRocket census was being scored against the wrong quantity, one-directionally: a total is never smaller than its own vertical component. Comparing per era took the OpenRocket median **7.8% → 1.2%** and the whole metric **8.3% → 2.0%**, with no change to the solver — and the one-sided bias the census could never explain went with it, from "86 of 92 descend slower than stored" and a −8.2% signed median to 66 of 92 and −1.0%. **The transferable point, which is why this row is worth more than its fix:** three separate accuracy "gaps" on this metric — 3.0%, 8.3%, 21.9% — were all the same defect, a stored number and a computed number that were not the same physical quantity, and none of them was visible as a physics error. A file format that does not state its own conventions makes every stored figure a claim to verify from the writing tool's source, not a value to read. |

---
| 32 | OpenRocket / RockSim | **A parts catalogue that is present offline and costs nothing to carry** — the desktop tools ship their component database inside the install, so it is always there | Shipped 2026-08-02 as a SEPARATE lazily-fetched chunk: 85 KB gzipped against a 343 KB whole-app budget, so first paint is unchanged, and the service worker precaches it like every other asset, so it is offline from the second visit. Verified from the built export — the chunk carrying `BT-60` is referenced by no prerendered document | `BETTER` | The desktop tools pay for their catalogue at install time, which is free for them and impossible for us; a web tool that put 85 KB on every first load to carry a table most sessions never open would be paying the cost on the wrong side. Lazy + precached is strictly better than both: nothing on first paint, everything offline afterwards. **The thing to watch** is that this is the app's FIRST dynamic `import()`, so the pattern is now precedent — the next big table should copy it rather than static-importing. `lib/motors/catalog.ts` (26.8 KB gz) is still statically imported and is the obvious next candidate. |

## Standing conclusion — where Loft actually wins

Keep this honest and current; it is what the landing surface and the README should say, and right now
they do not say it.

1. **Nothing to install, nothing to pay, nothing to sign up for, and it works offline.**
2. **It reads the file you already have** — whichever tool made it.
3. **It shows you more than one answer** — its own, the file's, an external oracle's — and flags
   disagreement instead of hiding it.

Where it loses today: the editor is younger than OpenRocket's, there is no parts or materials
catalogue, the physics is not yet 6-DOF, and — the one a flyer sees first — **it is shaped like one
long page instead of an application.**
| 35 | RASAero II, RocketPy, OpenRocket, RockSim | **Where a parachute's drag coefficient comes from, and whether the tool says.** All four fly a canopy off a Cd nobody measured, and they differ in how honest they are about it. **RASAero II** is the best of them: *"the default parachute drag coefficient (CD) of 1.33 is based on a study of high power rocket descent rates as a function of parachute size, but the User has the option of entering his own parachute CD based on descent rate flight data"* — a stated default, a stated basis, and a way to change it, with the reference area pinned as *"the diameter with the parachute laid flat on the ground"*. **RocketPy** requires `cd_s` (Cd × area) outright and never derives it; its `drag_coefficient` default of **1.4** is cited to NASA SP-8066 and is used only to back a radius out of `cd_s`, with 0.75 flat-circular and 1.5 extended-skirt listed. **OpenRocket** defaults to **0.8** with `cdAutomatic = true` and its own source says `// TODO: HIGH: Better parachute CD estimate?`; its `.orc` schema defines `<DragCoefficient>` but **0 of the 15 vendored files state one**, so every catalogue chute takes the default. **RockSim v8's parachute screen has no Cd field at all** — it is implied by Shape and never shown, while the Streamer screen beside it does expose one | **Loft reads a `cd` from the design file and, as of 2026-08-03, carries it faithfully through a catalogue pick rather than inventing one** — measured across the corpus, 22 of the 37 parachute nodes state a coefficient explicitly (1.5 ×10, 1.55 ×4, 0.75 ×4, 2.2 ×2, 1.34, 0.61) and the rest say `auto`, which the importer maps to 0.8. **RESOLVED 2026-08-04 — the coefficient is readable, editable and attributed on `/design`.** It shows the figure being flown, names its origin in the flyer's terms (*the design file's own figure* / *Loft's fallback, because the file states none* / *Loft's own, for a canopy authored here* / *your own figure*), and takes an edit that flows through a re-fly and survives the `.ork` round trip. Every fallback's basis is published on `/docs/limitations` — including the two that are honest admissions of having none — and the five derived readouts a FALLBACK feeds (descent rate, drogue descent, ground-hit speed, arrival speed, landing energy) carry the `extrapolated` marker from row 33, which is the interaction the original note predicted. Earlier state: the flyer could not see it or change it on any surface, and the default's basis was stated nowhere | `RESOLVED`, with one clause of R9's own wording refuted | Added 2026-08-03, from a probe aimed at the surface R8 increment 6 was about to touch. **Resolved 2026-08-04 by R9 increments 4 and 5, and the honest position against the field is now: Loft states MORE about this number than any of the four.** RASAero II states a default and its basis; Loft states a default, its basis, whether the figure in front of you IS the default, and — where it is — marks every number computed from it. **What R9's own *done when* got wrong, recorded because a roadmap clause is not evidence:** it asked for the origin to be named as "file value / catalogue part / Loft's default", and *catalogue part* cannot occur. Measured: **0 of the 151 catalogued canopies publish a drag coefficient**, so a catalogue pick leaves the field exactly as it found it — which `components/PartPicker.tsx` already told the flyer in words before this shipped. Naming it would have invented a provenance the data cannot support. The four values that DO occur are the file's, Loft's fallback, Loft's own for a canopy authored here, and the flyer's. **Still owed, and it is the smaller half:** RASAero II's documented 1.33 remains uncopied, deliberately — it fires on 0 of the corpus's RASAero designs, so moving it would change no flown number and could be validated against nothing. **This is the one number in the recovery chain a flyer cannot reach**, and it sets landing speed and landing energy — measured on `demo-single-deploy.ork`, swapping the catalogue's largest canopy for its smallest moves ground-hit speed from 2.16 to 18.15 m/s, and every one of those figures rides on a coefficient nobody chose. Two of the four competitors state where their default came from; Loft states nothing. **The smallest slice is a field plus a cited default**, not a model: RASAero II's 1.33 and RocketPy's SP-8066 1.4 are both citable, and `DESIGN.md` §6 already requires a reference value to name its source. It interacts with row 33 — a defaulted Cd is exactly the shape of assumption the `extrapolated` marker exists for, and the marker belongs on the DERIVED readouts (descent rate, arrival speed, landing energy) rather than on the input. Verified from the RASAero II Users Manual, RocketPy's `Parachute`/`Function` source, OpenRocket's `Parachute.java` and the vendored `.orc` files, and the RockSim v8 Program Guide (`UNVERIFIED` for RockSim v10/v11). |
| 36 | OpenRocket, RockSim | **How the parts catalogue is REACHED, and what it remembers between opens.** OpenRocket opens its preset chooser AUTOMATICALLY when a component is created, with a per-type *"Always open this dialog"* preference a flyer can turn off (`ComponentPresetChooserDialog`, `l10n/messages.properties`), and its dialog persists column widths, sort order and the filter across sessions through `Preferences`. Apogee's public RockSim help describes the same auto-open idiom verbatim, though no screenshot of its dialog is published (`UNVERIFIED` for RockSim's columns, favourites and sorting). RASAero II has no component catalogue at all — its geometry is typed — which is why it is absent here | **Loft's catalogue is opt-in and stateless.** It is reached only by finding a *Pick a real …* button beside the fields, so a flyer who does not know it exists never sees 2,990 parts; and `components/PartPicker.tsx` holds `open`, the search text, the vendor filter and the caliber toggle in component-local `useState` (measured 2026-08-03), while `lib/session.ts` — which persists the edit bag, the weather, the scenario and the sim index — carries none of them. Picking tubes for a four-tube build means retyping the vendor filter four times. **Where Loft is ahead and it is untracked until now:** editing a preset-derived dimension in OpenRocket calls `clearPreset()` (`RocketComponent.java`), silently dropping the attribution; Loft keeps the pick, narrows the claim to what still comes from the vendor, and keeps the way back — so a flyer always knows which numbers are whose | `GAP` | Added 2026-08-03 from a competitive probe aimed at the surface R8 finished. Everything about OpenRocket here is read from its `unstable` source and `l10n/messages.properties`, not from memory. **The auto-open half is a product decision and not obviously theirs to win** — Loft's authoring gestures size a part from the design immediately, so a flyer gets a working part and can then upgrade it, where OpenRocket's dialog interrupts every creation. **The forgetting half is not a decision, it is the "controls that forget" tell on the newest surface in the app**, and it is the cheaper and clearer of the two: the filter state belongs in `lib/session.ts` beside everything else that survives a reload. Filed in `BACKLOG.md` with the measurement. |
| 37 | OpenRocket, RockSim | **A stored flight records whether recovery actually DEPLOYED — both formats say so, and Loft read neither until now.** Verified 2026-08-04 by reading the corpus, not the docs. OpenRocket writes a per-step event timeline into each saved run's `<databranch>`, including `<event type="recoverydevicedeployment"/>`: **77 of the 91 stored `.ork` flights here carry one, and not one logs events without one** (the other 14 are summary-only saves with no timeline at all). RockSim states it per device as `<HasDeployed>` inside each run's `<SimulationEvents>` — distinct from the identically-named flag inside `<Booster1Staging>`, which is a STAGE separation — corroborated by an undocumented `<FinalState>` enum on 17 of 17. | `lib/ork/adapt.ts` (`orkRecoveryDeployed`), `lib/rkt/adapt.ts` (`recoveryDeployed`), `lib/corpus/sweep.test.ts` | `HAVE` | **The capability is reading it; the differentiator is what it is FOR.** Neither tool publishes an accuracy census, so neither has a reason to separate a canopy descent from a lawn dart — they simply store the fact. Loft publishes one, and until this row it was averaging 12 no-recovery descents (83–162 m/s) in with 70 canopy ones (a few m/s) and calling the result "how close Loft's landing speed is". Ground-hit velocity now reads **0.8%** over the non-ballistic runs (80 of those 82 after 2026-08-09, two being exact repeats of a run already counted; it read 1.3% until the ring-bore parser fix later the same day) and 14.9% over the 12 ballistic ones, both published. **The transferable lesson, and the third time this milestone has hit it: a format that stores a number without stating what flight it describes will be read wrong, and the answer is in the file rather than in the documentation.** |
| 38 | OpenRocket, RockSim | **What "optimum ejection delay" MEANS — two formats, two different flights, and neither says which.** Established 2026-08-05 from the corpus's own arithmetic rather than from documentation. **OpenRocket stores the FREE-COAST delay**: its `optimumdelay` attribute is the flight log's `timeToOptimumAltitude` minus the last burnout event, exact on **73 of 73** stored simulations here — where the obvious reading, apogee minus burnout, matches only 56 of 73. Optimum altitude is the altitude the vehicle would have reached with nothing out, so an early deployment does not drag the figure down with it. **RockSim stores the AS-FLOWN delay**: `<OptimalDelay>` is exactly `<TimeToApogee> − <TimeToBurnout>` of the run it sits in — 4.765 = 10.8263 − 6.06125, 4.53 = 5.98125 − 1.45125, 1.34375 = 3.65375 − 2.31, 17.9325 = 20.2425 − 2.31, on every stored simulation in all four corpus RockSim designs, to each file's own stored precision. On a design whose canopy opens at burnout those two definitions describe different flights. | **Loft reports the free-coast figure — the right one for a flyer buying a delay — and was comparing it against both.** `lib/sim/run.ts` substitutes a recovery-free coast whenever a device opened before apogee, which is correct for OpenRocket and wrong for RockSim: `FullScaleModelTH.rkt`'s four `[L1940X-0]` runs open at burnout, so RockSim's own apogee is 3.65 s and its stored delay 1.34 s against Loft's free coast of ~16 s. Four census rows read **+1107%** for a delay model that was not wrong. `StoredSimulation.optimumDelayBasis` now records which flight the file describes and `compareToStored` scores the matching figure: those rows move to **−21%**, and the worst optimum-delay disagreement anywhere in the corpus goes **1107% → 59%** | `HAVE`, and the gap it exposed was the comparison rather than the model | Added 2026-08-05 by R10 Size item (5). **The published median did not move, and that is the finding.** Four rows of 84 changed by a factor of eighteen and the census median stayed at 2.5% — which is what a median is for, and why it was the wrong instrument to catch this. The suite now asserts the WORST optimum-delay row as well as the median, because a row comparing two different flights is a different defect from a metric that is simply off, and only one of the two is visible to a median. **This is the fourth time R10 has found the same shape** — after ground-hit velocity's frame, the 24.12 era split, and the ballistic/canopy population — and the pattern is now specific enough to state as a rule: *where two tools store a figure under the same name, assume they mean different flights until the file's own arithmetic says otherwise.* Verified from the corpus files directly; no claim here rests on either tool's documentation, because neither documents it. |
| 39 | OpenRocket, RockSim | **The component tree as a SURFACE the flyer sees, selects in, and adds from.** OpenRocket's editor is a nested outline beside a to-scale view: parts sit under their hosts, selecting either one selects both, and an always-visible category palette offers what can be added — greying illegal targets rather than hiding them. Split out of row 7 on 2026-08-08, because the four mechanics shipped and the surface did not | **`GAP`, and measured rather than asserted.** Loft's model already carries the tree — every component has `children` and a parent-relative `Placement` — and no UI shows it. What a flyer gets instead, across the 27 corpus `.ork` designs (459 components): a **collapsed-by-default** flat table with no parent column, in which **347 of 459 (75.6%) sit at depth ≥ 2**, max depth 4; a diagram that gives a hit target to body outlines, fin sets and mass objects only, so **256 of 459 (55.8%) are selectable ONLY through that collapsed table** — 69 centring rings, 41 parachutes, 30 inner tubes, 27 couplers, 27 bulkheads, 24 shock cords, 16 launch lugs, 12 engine blocks, 9 rail buttons, 1 streamer; a property surface of five always-visible `fieldset`s below the diagram, every field addressing a component by ROLE rather than id, leaving **215 of 459 (46.8%) with no editable field of any kind**; and an add affordance behind a single `kind === "bodytube"` guard, so **~85% of parts answer "what can I add here?" with silence** | `GAP` | **R12**, from `OWNER-NOTES.md` `ON-6`/`ON-7` — the owner described this destination in a user's words, from the outside, having used the tool: *"there is a tree of parts from top to bottom in which components such as a payload or a mass or a parachute can be under a coupler or tube."* CLEAN-ROOM: the interaction model is taken from OpenRocket's published documentation and observable behaviour; its GPL source is not read. **Two questions this row did not settle and R12 must: one is now settled on row 40** (the property surface stays live-commit, not transactional — the live re-fly is what Loft has that the desktop tools do not). Still open: whether "start from scratch" means OpenRocket's empty stage or Loft's six-part flyable starter. **What Loft has that OpenRocket does not, and must not lose:** the flat what-if patch re-flies the design live on every keystroke, so the tree has to grow beside it rather than replace it in one step. **PARTIAL 2026-08-08 — the tree is now VISIBLE**: `flattenRocket` carries `depth`, `parentId` and `stageIndex`, and the parts list renders the nesting (an indent in design order, the host named in words in every order, and the kind exposed for selection). 569 parts across all 35 design files, 419 of them nested, verified consistent by a corpus case. Still `GAP` because seeing is not editing: selection does not yet drive the property surface, the list is still collapsed by default, and the add affordance is still behind one body-tube guard. |
| 40 | OpenRocket | **The per-component PROPERTY DIALOG, and the verbs that sit beside the tree.** Verified 2026-08-08 from OpenRocket's published documentation and tutorials only — its GPL source is not read. Four things Loft's editor has no equivalent of. (a) The design tab is *three* bands, not two: *"(Left) The component tree… (Middle) Component action buttons… (Right) Component addition buttons"* — so every verb has a permanent home beside the outline, and Loft has none at all, only a hit target on a diagram row. (b) **Add is always child-of-selection**: select a node, click a palette button, and the new part lands under it; there is no separate "add sibling" gesture, and illegal kinds are *"shaded until available"* rather than hidden, which teaches the legality instead of concealing it. (c) The property dialog is **tabbed and type-specific** — a body tube gets *General, Motor, Override, Appearance, Comment* — and it is **transactional**: a preference exists for *"Show confirmation dialog for discarding component changes… if you click the Cancel button"*, so an edit can be abandoned. (d) Every operation has **four parallel routes** — buttons beside the tree, the Edit menu, keyboard shortcuts named in that menu, and a right-click contextual menu — and the node verb set includes **duplicate** as a first-class operation distinct from copy-and-paste | **Loft has (b) partially and none of (a), (c) or (d).** Measured 2026-08-08: adding is behind a single `kind === "bodytube"` guard in `components/GeometryInspector.tsx`, so the selection is the insertion target only when the selection happens to be a tube; the property surface is five always-visible `fieldset`s at the bottom of `/design`, live-commit on every keystroke, addressing components by ROLE (`primaryNose`, `primaryBodyTube`) through `AIM_SLOTS`; there is no duplicate verb, no contextual menu, and no keyboard route to any structural operation other than Tab-to-the-button. **Where Loft is ahead and it constrains the answer:** the flat what-if patch re-flies the whole design on every keystroke, so a flyer sees the flight move as they type — a transactional dialog would have to give that up or fake it | `GAP` | Added 2026-08-08 from a competitive probe aimed at R12's next increment. **It settles one of the two questions row 39 left open, and settles it against OpenRocket:** Loft's property surface stays **live-commit**, not transactional. The reason is measurable rather than aesthetic — the live re-fly is the thing Loft has that the desktop tools do not, `lib/model/history.test.ts`'s undo already covers "I did not mean that" across the whole design, and a per-component Cancel would need a second, component-scoped history that could disagree with the global one. Recorded in `ROADMAP.md` under decisions taken without the owner. **The other question — empty stage or flyable starter — is still open.** (a) and (d) are the parts worth taking next: a verb band beside the tree, and a keyboard route to each verb, which `DESIGN.md` §8 already requires and which the diagram-only affordances do not provide. |
| 41 | OpenRocket | **WHICH component kinds get a property dialog at all, and what each dialog holds.** Verified 2026-08-09 from OpenRocket's own published source and its message catalogue — the config classes and `messages.properties`, read for the FIELD LISTS and the resolution rules, not for implementation. Four findings. (a) **Every kind has one**, resolved by naming convention rather than a registry: `ComponentConfigDialog` appends `Config` to the component's class name and walks up the superclass chain, so a kind with no dialog of its own inherits its parent's rather than falling through to nothing. (b) The ring family is **three dialogs over one shape**: `CenteringRingConfig` builds `Thickness / Outer diameter / Inner diameter`; `BulkheadConfig` is the smallest in the program at `Thickness / Diameter` alone; `ThicknessRingComponentConfig` serves tube coupler AND engine block at `Length / Outer diameter / Inner diameter / Wall thickness`. **A plate has a THICKNESS and a tube has a LENGTH — one model field, two words.** (c) `InnerTubeConfig` is the richest inner component: four tabs, `General` reusing the ring fields plus `Motor`, `Cluster` and `Radial position`. (d) Every dialog gets three tabs free from the base class — `Override`, `Appearance`, `Comment` — so mass override and a per-part note are universal rather than per-kind | **Loft now has (b) in full and none of (a), (c) or (d).** Shipped 2026-08-09: one `AIM_SLOTS` entry covers all five internal kinds with `Length or Thickness / Outer Ø / Bore Ø`, and the axial label follows the same plate/tube split from the same source. That took the parts no field describes from **249 of 569 corpus parts to 55**. What is still missing is bounded and countable: the 55 are shock cords, launch lugs, rail buttons and a streamer, all `MinorComponent`; a motor mount's own overhang and cluster count have no control; and there is no mass override or per-part comment anywhere. **Where Loft is ahead:** every bound is enforced against the part's actual host and advertised on the field, so a coupler cannot be typed wider than the tube it sits in — OpenRocket's dialogs take the number and let the drawing show the overlap | `GAP` | Added 2026-08-09, and it is the row that scoped R12's third increment rather than one written after it. **Reading the source for FIELD LISTS is a different act from reading it for implementation, and the clean-room invariant permits the first.** The decision it settled: one aim slot for five kinds, because `RingComponent` and `InnerTube` are one shape in Loft's model even though OpenRocket splits them across three dialog classes — Loft follows the model and takes only the vocabulary. **CORRECTION 2026-08-11 to this row's own claim:** it says "a motor mount's own overhang and cluster count have no control", and the CLUSTER COUNT half is stale — a `Motor cluster` input (min 1, max 12, *"How many motors the mount holds"*) exists, in the **Motor** fieldset rather than on the mount's own property panel, which is a placement complaint rather than an absence. **Overhang genuinely has none**, and it is imported, FLOWN (`lib/sim/setup.ts:246`) and exported: 35 `<overhang>` across **27 of 27** corpus `.ork` (21 nonzero) and 33 `<EngineOverhang>` across **4 of 4** `.rkt`. **Next from this row:** (d) is the cheapest and the most useful, because a stated mass override is how a flyer reconciles a design with a scale, and Loft honours `overrideMass` on import already while offering no way to set one. **(d)'s MASS half is RESOLVED 2026-08-10, and it went further than parity.** The override now reaches every aimable kind: the nose cone and the body tube were the last two, at **13 body-tube and 10 nose-cone** non-Loft masses across the 35-design corpus. (This row's own earlier count of 26 for the nose cone did not reproduce — two independent counts over the same files say 10, and the body tube's 13 reproduced exactly.) **Where Loft is now ahead of OpenRocket on this control:** OpenRocket's Override tab is present on every component whether or not the figure it writes can reach the flight, so inside an assembly that states its own subtree weight the tab still takes a number that changes nothing. Loft withholds the field there and names the assembly carrying the weight — 42 aimable parts across 4 of the 35 corpus designs, which its own parts table had always labelled *"in ⟨assembly⟩"* while the property panel showed a live box over a placeholder of 0. **What is still open in (d):** the per-part COMMENT, which is row 42's edit half and needs a keyed bag entry the flat patch cannot express. |
| 42 | OpenRocket · RockSim | **The per-component COMMENT, and what a round trip through Loft does to it.** OpenRocket gives every component a `Comment` tab from the same base class the `Override` tab comes from, so a note can be written on any part, and it is persisted in the file as `<comment>`; the wiki records that hovering a component surfaces *"the name and mass of the component on the first line and the text entered in the comment box beginning on the second line"*, which is what makes a note worth writing — it is readable without opening a dialog. RockSim carries a per-part `<PartDesc>` (40 parts across 9 kinds in the corpus: "Airframe tube", "KwikSwitch MMT 29mm", "Transition T60 to T80 2.25 in long"), though whether its UI presents that as a note or as a catalogue description is UNVERIFIED. RASAero has one design-level `<Comments>` and nothing per part; RocketPy has no components to comment on | **Loft neither reads it nor writes it, and that is data loss rather than a missing feature.** Measured 2026-08-09 over the corpus: **40 non-empty `<comment>` elements across 18 of the 27 `.ork` files**, and `grep -rn '"comment"' lib/ork/ lib/rkt/` returns nothing — so a flyer who imports a commented design, edits it and exports it gets a file with every note stripped, silently. `ComponentBase` has `name` and no note field at all, so unlike `overrideMass` — which the model, the solver and both halves of the file I/O already carry, and which needs only a control — this one needs a model field, an importer, an exporter and session persistence before any UI | `GAP` | Ranked above the mass-override control on severity even though it looks like the smaller feature: the override's absence costs a flyer a capability they never had, and this costs them work they already did. The hover surface OpenRocket uses is not available to Loft — `DESIGN.md` §8 forbids hover-only affordances outright — so the note needs a route that works under a thumb, which is a design question and not a port. **Next from this row:** the model field and the two adapters first, so an import→export round trip stops destroying notes; the control and the surface after, with the mass override, since they share the property surface and the same per-part-metadata shape. | **PARTLY RESOLVED 2026-08-09 — the FILE half is closed and the EDIT half is not.** The model carries `comment` on the component, the stage and the rocket; the `.ork` adapter reads it from the one function every kind's parse spreads, the `.rkt` adapter reads both of RockSim's spellings (`PartDesc` per part, `Comments` on the design), and the exporter writes it. Pinned by `lib/corpus/sweep.test.ts`'s *carries every note a real design wrote*, which round-trips **81 notes across 22 of the 35 designs** as an exact multiset — this row's own 40 was right in total and its breakdown was not: 16 of the 40 are DESIGN-level, 1 is on a stage and 23 are on components. What is still missing is AUTHORING one, which needs a keyed entry the flat edit bag cannot express — the same obstacle the mass override hits, filed in `BACKLOG.md`. OpenRocket's hover-to-read is unavailable to Loft either way: `DESIGN.md` §8 bans a hover-only state outright |

| 43 | OpenRocket · RockSim | **Whether a tool says that a mass was STATED rather than computed — and both of them do.** OpenRocket's component dialog carries an `Override` tab beside the geometry, so the flyer who set a mass sees the checkbox that set it, and the file records the fact as a distinct element (`<overridemass>`) rather than as a value in the ordinary mass field. **RockSim goes further and stores BOTH figures side by side**: `<CalcMass>`/`<CalcCG>` sit next to `<KnownMass>`/`<KnownCG>` on **67 of 67 parts across all 4 corpus `.rkt` files**, so the computed and the entered number are both always available to show. The spreads are not cosmetic — `FullScaleModelTH.rkt`'s tube coupler states 984.0 g against a computed 70.6 g, a factor of 13.9 | **Loft flies 91 stated masses and marks none of them.** Measured 2026-08-09 over the corpus: 64 `<overridemass>` across 15 of the 27 `.ork` files, spanning 15 kinds from `stage` to `railbutton`, plus 27 `KnownMass` > 0 across all 4 `.rkt` files. `massCell` in `components/GeometryInspector.tsx` renders every one as a bare figure whose only provenance is *in &lt;assembly&gt;* for a subsumed part, and the parts CSV carries the same bare number — so a mass the designer weighed on a scale and a mass Loft derived from a density read identically on the surface a flyer checks their import against. `DESIGN.md` §6 requires a reference value to name its source, and this is 91 values that do not. **The obstacle is a real one and it is why this is a row rather than a fix:** `overrideMass` means two different things depending on which adapter set it — `lib/ork/adapt.ts` sets it only from a genuine `<overridemass>`, while `lib/rkt/adapt.ts` synthesises one on every structural part from whichever figure RockSim's own `<UseKnownMass>` selects — so the field cannot drive the marker without a parse-time provenance field beside it, in the shape `CdProvenance` already takes for a drag coefficient. **RESOLVED 2026-08-10, and the provenance field is what resolved it.** `massFrom` on the component base carries three cases — `"stated"` (the design gives the weight outright), `"tool"` (the source tool's own computation, carried through rather than recomputed) and absent (Loft derived it from geometry and material) — set by all three importers at the point each reads the figure, so the marker cannot come adrift from the number. Census over the corpus: **108 stated, 60 carried from the source tool, 401 computed here**. The parts table marks the first two and gains a *Mass from* column that says them in words, which is also what the CSV exports; the key sits in the table's own caption rather than in a tooltip, because §8 forbids a state a phone cannot reach — a first version put it in a `title` and the touch suite counted it as two new hover-only states. What Loft still does NOT do, and RockSim does, is show both figures at once: the computed mass is discarded once the stated one wins, so a flyer cannot see the 13.9x disagreement the way a RockSim user can. That is the next thing here, and it needs the model to carry both. |
| 44 | RockSim · OpenRocket | **What a tool stores for a flight event that did not happen — and whether the stored number can be told apart from a measurement.** Verified 2026-08-10 from the corpus files themselves, not from either tool's source or UI. **RockSim writes a `deploymentVelocity` for a run its own file marks as not-deployed**: `rocksimTestRocket1.rkt [E6-2]` carries `recoveryDeployed = false` and a stored 33.4 m/s, because the ejection charge fires whether or not anything comes out of the tube — so the figure is real, and it answers *"how fast was it going when the charge went off"* rather than *"how fast was it going when the canopy opened"*. The file states both facts, in separate elements, so a reader who consults the deployment mark can tell which question the number answers. OpenRocket carries the same distinction differently: it writes a `recoverydevicedeployment` event into the flight log per device, so an absent event is the absence of a deployment rather than a zero | **Loft has no reading for a charge that fires with nothing out, and until 2026-08-10 it published the gap as a disagreement.** `FlightSummary.deploymentVelocity` times the opening of a *device*; a flight where none opens carries 0, which `simulate.ts` documents as a sentinel. `compareToStored` scored that 0 against RockSim's 33.4 and published **"RockSim 33.4 m/s · Loft 0.0 m/s · −100%"**, and folded it into the panel's headline mean absolute error — which reported that design's error as **48.74%** where its comparable metrics disagree by **42.33%**. Loft now withholds a metric whose event never happened and names it under the table, so the row is only ever a disagreement between two answers. **What it still cannot do is answer the question RockSim answered:** Loft emits no event for a plugged charge, so it has no speed to put beside the 33.4 | `GAP` | Added 2026-08-10, from gating the sentinel rather than from a probe — the competitive fact was found in the file that exposed the defect. **Loft is now ahead on the honesty and behind on the capability, and those are separable.** The honesty half is shipped and pinned by a corpus census that names the one withheld row. The capability half is a real feature with a real audience: a flyer whose charge fires early wants the speed it fired at, and Loft models the plugged case explicitly (`simulate.ts` carries `plugged` and `ejectionPlugged`) without ever reporting a figure for it. **Next from this row:** emit an ejection event with its velocity whether or not a device opens, then compare THAT against a stored figure on a run the file marks not-deployed — which turns a withheld row back into a real comparison against the quantity the file actually holds. |
| 45 | OpenRocket · RockSim · RASAero II | **What a tool does when a design states ONE weight for the whole airframe and no per-part masses — and whether it will then let you type a per-part weight anyway.** The three tools differ at the format level rather than at the UI level, which is why this row exists. **OpenRocket** and **RockSim** are per-part by construction: every component owns a mass, and an override is an explicit, per-component act (`<overridemass>` with its own `overridesubcomponents` flag; `<KnownMass>` beside `<CalcMass>` with `<UseKnownMass>`), so "the whole weight" is never a single figure that a part weight could be added to — the question this row asks cannot arise there. **RASAero II** is the opposite: the `.CDX1` format states a launch weight and no per-part masses at all, so a per-part weight has nowhere to go and the tool does not offer one. Whether RASAero's own UI refuses, hides or silently ignores such an entry is `UNVERIFIED` — it was not run; the format's shape is read from the corpus files | **Loft is the only one of the four that offers per-part weights on ALL of them, and that is a real capability with a real hazard attached.** A `.CDX1` becomes one point mass standing for the airframe, and until 2026-08-11 four separate controls would add a typed weight straight to it. Measured on the bundled sample and the corpus: a canopy weight took `Show-off.CDX1` 0.4536 → 0.9536 kg and its margin 12.81 → 9.28 cal; a material pick took the bundled design 8.2649 → 25.5895 kg — a tripling, because the format states no wall thickness either, so each tube was flown as a solid rod — moving the margin 3.06 → 4.1 cal through a live select one click from the front door. Loft now refuses per-part weight on the parts that stated figure already contains, names it in words under each withheld control, and **still accepts one on a part the flyer AUTHORS**, which the stated weight provably cannot contain | `BETTER`, with the caveat that the advantage only exists because the refusal is now correct | **The capability is genuinely ahead and the row is worth keeping for the reason it is ahead.** RASAero flyers have no way to say what their nose cone weighs, in any tool, because their format has no field for it — so a scale reading is the only possible source and Loft is the only place it can go. What the other three get for free from being per-part, Loft has to earn with a guard, and it took three attempts: the guard shipped per-field twice (nose and tube, then not the canopy or the fitting) and then design-wide once, which broke authoring on exactly the format that needs it most. It is one table now — `PER_PART_MASS_FIELDS`, whose expected keys `lib/model/edit.test.ts` derives from the aim registry — so a seventh mass field fails the build rather than double-counting. **Next from this row:** the `overrideCGx` twin, which is carried end to end and honoured over the computed CG with no control anywhere (12 `<overridecg>` on 5 `.ork`, 67 `<KnownCG>` across the `.rkt`) — the same five pieces against a different field, and the same lumped-airframe question to answer on the way in. |
| 46 | OpenRocket · RockSim | **Letting the flyer state a component's BALANCE POINT, not just its weight — and saying whose figure it is.** **OpenRocket**'s component Override tab carries mass and CG side by side: *Override center of gravity* takes a station measured from the front of the component, with its own *Override for all subcomponents* flag, and writes `<overridecg>` beside `<overridemass>`. **RockSim** does the same through `<KnownCG>` gated on `<UseKnownCG>`, beside `<KnownMass>`/`<UseKnownMass>`, and its own `<CalcCG>` is kept alongside so the file records both the tool's answer and the user's. Both treat the two measurements as one act, which is what a builder actually does: a part comes off the bench, it goes on a scale, then it goes on a knife edge. **RASAero II**'s `.CDX1` has no per-part CG field at all — the same shape as row 45's mass question — so the capability cannot arise there. Neither OpenRocket's nor RockSim's UI behaviour was driven; the format shapes are read from the corpus files, so the UI details above are `UNVERIFIED` | **Loft has HONOURED a stated CG since its first importer and could not write one until 2026-08-12.** Census over the 35-design corpus: **79 per-part override sites across 9 of 35 files — 38 enabled, 41 disabled** (12 `<overridecg>` on 5 `.ork`; 67 `<UseKnownCG>` on 4 `.rkt`, 26 of them set) — and **14 components across 7 designs** come back with `overrideCGx` set. An earlier draft of this row said 84 across 12 files, which counted 5 RASAero `<SustainerCG>` elements — a whole-airframe launch CG that Loft imports as a placement, not as a per-part override, and which this same row says two sentences earlier cannot arise in that format. Honouring them moves the static margin on 6 of those 7 designs, by up to 1.011 cal (`rocksimTestRocket1.rkt`). `MassBreakdown` has published a *CG from* provenance column since 2026-08-11, so the design's claim was already distinguishable from Loft's arithmetic; what was missing was the flyer's own. Two controls now — nose cone and body tube — measured from the part's own fore end as OpenRocket asks for it, marked `cgFrom: "flyer"`, applied after a catalogue pick and a caliber scale so a knife-edge reading survives both. **Two things Loft does that neither of the others does.** It is BOUNDED: a station past the end of the part is clamped to that part's length rather than flown, because it cannot mean anything — 63 of 70 corpus parts are short enough for the bound to fire. And it is REFUSED where it would do nothing: `statedCGReachesDesign` perturbs the part and asks the solver whether the design's balance point actually moves, so on the 8 of 70 controls where the part carries no weight of its own for a station to place — all 8 are RASAero shells, which the format gives no material — the field says so instead of taking a number that changes nothing | `PARITY` on the capability, `BETTER` on the two guards | **The refusal is the part worth keeping, and it is the part that could not be copied from the mass side.** For a stated WEIGHT the hazard is a double-count and the predicate is "does an ancestor already state this part's mass". For a stated CG there is nothing to add to, so the hazard is a silent no-op — and the two predicates point OPPOSITE ways on the same part: on a stage-level override (3 of 35 designs) the lump's CG is recomputed from every subsumed part, so a per-part CG is live while a per-part mass on that same part is dead. Reusing `statedMassHolder` would have greyed out a working control on three of the four lumped designs. Deriving the answer from the solver by perturbation rather than by reasoning about the tree is the transferable move. **RESOLVED on the reading half, 2026-08-12:** the parts table now carries *Balance* and *Balance from* beside *Station* and *Mass from*, both in the CSV, so the surface whose stated job is *did Loft read my rocket right?* shows where each part acts and whose figure that is — and every per-part CG in the app now answers the provenance question through one describer. **Still open from this row:** the two importers disagree about which component kinds may carry a stated CG (`lib/rkt/adapt.ts` restricts to nosecone/bodytube/transition, `lib/ork/adapt.ts` accepts any), which is filed in `BACKLOG.md`. |
| 47 | OpenRocket | **What a stated balance point on a component actually MEANS when that component has a shoulder — and whether the tool and the flyer agree about which part is being balanced.** **OpenRocket**, verified 2026-08-13 by reading `RocketComponent.java` and `Transition.java` at `release-24.12` (clean-room: the semantics were read, no code was taken): `getCG()` returns `getOverrideCG().setWeight(getMass())` when the CG is overridden, and `getOverrideCG()` is `getComponentCG().setX(overrideCGX)` — the x replaced WHOLESALE on a centroid that `Transition.calculateProperties()` builds by summing fore cap, fore shoulder, transition, aft shoulder and aft cap. **So a stated CG is the WHOLE part's balance point, shoulder included, and a stated MASS keeps that same centroid and rescales the weight only.** Neither is documented in prose anywhere we could find; the file format states the element and not its meaning, which is why this row records the source rather than a manual page. | **RESOLVED 2026-08-13.** Loft had both branches wrong, in opposite directions, and now reproduces OpenRocket's precedence term for term (`lib/sim/mass.ts`). A stated CG used to set the shell's centroid with the shoulder blended in aft of it — the part acted up to 133 mm behind the station stated; a stated mass used to drop the shoulder from the CG entirely. Measured across the 35-design corpus: 4 designs move dry CG, all aft, 1.43–4.32 mm, with no mass change on any, and all twelve published accuracy medians byte-identical. **Where Loft is now AHEAD:** it says whose figure a balance point is (`cgFrom`) on every surface that shows one, which OpenRocket's Override tab does not — it stores the number and not its provenance. **Where OpenRocket is still ahead:** its override applies to any component, where Loft offers the control on the nose cone and the primary body tube only. | `RESOLVED`; the any-component clause stays open | The bound moved with the meaning: a whole-part centroid can sit behind the body's own base, so `[0, length]` was wrong and `statedCGBounds` is the bound now. 20 of the 854 catalogued cones balance past their base, which is where a flyer meets this — a catalogue pick is one click from the front door. UNVERIFIED: whether RockSim's `KnownCG` carries the same whole-part meaning; its published spec states the field and not its datum, and both corpus `.rkt` overrides are inside the body either way, so nothing here distinguishes them. |
| 48 | OpenRocket · RockSim · RASAero II | **What sets a design's REFERENCE DIAMETER — the length every stability figure is quoted in calibers of.** **OpenRocket** exposes this directly: a rocket's *Reference type* is `MAXIMUM`, `NOSE` or `CUSTOM`, and its `.ork` carries `<referencetype>` beside an optional `<customreference>`. Read from the corpus rather than from a manual: 31 of the 35 corpus designs carry a `<referencetype>` element and every one of them says `maximum`. What OpenRocket measures the maximum OVER is `UNVERIFIED` — the format states the choice, not the population, and the semantics were not read from source for this row the way row 47's were. **RockSim** and **RASAero II** expose no equivalent selector in their formats at all; neither `.rkt` nor `.CDX1` carries a reference-type field, so a flyer moving between tools cannot state the choice OpenRocket lets them make. | **Loft implements all three types (`lib/model/geometry.ts`'s `referenceRadius`) and takes the maximum over EVERY component, internals included — which is not the same population as "the airframe".** Found 2026-08-14 as a Sev-1 on the authoring path: a catalogued coupler or centring ring pick wrote the vendor's outer diameter with nothing bounding it against the tube it goes inside, and 123 of the 236 catalogued couplers and 243 of the 497 rings are wider than the bundled starter's entire airframe. One pick took the reference diameter 54 mm → 289.8 mm; holding mass and CG identical, the reference alone moved static margin **0.6211 → 0.1157 cal**. A ring is invisible on the diagram — the silhouette walks the airframe only — so nothing on screen said the caliber had moved. **Refused now**, at the model and on the row that offers it. **Where Loft is AHEAD:** neither OpenRocket nor RockSim refuses an internal part wider than its host, and Loft now states in `/docs/methods` what `d_ref` is, which none of the three documents. | `PARITY` on the three reference types, `BETTER` on refusing a part that cannot fit | **The import route is still open and is deliberately not guarded: 0 of the 35 corpus designs set their reference from a non-airframe part.** A guard firing on zero real files is worse than nothing, so it is filed rather than built. Worth recording how that number was established, because the first attempt got it wrong: a probe that hand-rolled the accessor as `outerRadius ?? aftRadius` reported 1 of 35 and named an OpenRocket sample, because a `transition` has neither property and one widest at its FORE end read as its narrow end. `lib/model/geometry.ts` exports `outerRadius()` and answers for every kind. The fabricated figure reached the ledger, the handoff and a docs page before a pre-push review killed it. |
